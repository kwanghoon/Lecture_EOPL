let fst = proc(p : pairof (int -> bool) * int) 
            unpair (x,y) = p in x 
in 
let snd = proc(p : pairof (int -> bool) * int) 
            unpair (x,y) = p in y 
in 
let mypair = newpair (proc(x:int) zero?(x), 0) in 
  ((fst mypair) (snd mypair))
  