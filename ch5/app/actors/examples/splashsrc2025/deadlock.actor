proc(main)

    let a = new (proc(self)
                    letrec loop (d) = ready(loop) in ready(loop)) in
    let b = new (proc(self)
                    letrec loop (d) = ready(loop) in ready(loop)) in

    let g = proc a (x) 
                let (h,y)=x in begin print("g"); (h y) end in
    letrec h b (z) = begin print("h"); (g (h,z)) end in

    (h 10)